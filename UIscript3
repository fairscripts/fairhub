-- UI estilo pasta macOS - Fair Hub
-- Feito para rodar no Delta Executor

local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

-- Remover se já existir
if CoreGui:FindFirstChild("FairHubUI") then
    CoreGui.FairHubUI:Destroy()
end

-- Criar ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "FairHubUI"
gui.Parent = CoreGui
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Janela principal
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 300, 0, 200) -- metade do tamanho anterior
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -100)
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 30, 35)
mainFrame.BackgroundTransparency = 0.15 -- leve transparência
mainFrame.BorderSizePixel = 0
mainFrame.Parent = gui
mainFrame.Active = true
mainFrame.Draggable = true

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 8)

-- Barra lateral
local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0, 90, 1, 0)
sidebar.BackgroundColor3 = Color3.fromRGB(128, 0, 128)
sidebar.BackgroundTransparency = 0.15
sidebar.BorderSizePixel = 0
sidebar.Parent = mainFrame
Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0, 8)

local gradient = Instance.new("UIGradient", sidebar)
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 20, 80)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(160, 60, 160))
}

-- Título "Fair Hub"
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -10, 0, 20)
title.Position = UDim2.new(0, 5, 0, 0)
title.BackgroundTransparency = 1
title.Text = "Fair Hub"
title.Font = Enum.Font.SourceSansBold
title.TextSize = 16
title.TextColor3 = Color3.fromRGB(0, 255, 255)
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = sidebar

-- Botão Auto Farm
local autoFarmBtn = Instance.new("TextButton")
autoFarmBtn.Size = UDim2.new(1, -10, 0, 20)
autoFarmBtn.Position = UDim2.new(0, 5, 0, 30)
autoFarmBtn.BackgroundColor3 = Color3.fromRGB(90, 40, 90)
autoFarmBtn.BackgroundTransparency = 0.1
autoFarmBtn.Text = "Auto Farm"
autoFarmBtn.Font = Enum.Font.SourceSans
autoFarmBtn.TextSize = 14
autoFarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoFarmBtn.BorderSizePixel = 0
autoFarmBtn.Parent = sidebar
Instance.new("UICorner", autoFarmBtn).CornerRadius = UDim.new(0, 4)

-- Área principal
local content = Instance.new("Frame")
content.Size = UDim2.new(1, -90, 1, 0)
content.Position = UDim2.new(0, 90, 0, 0)
content.BackgroundColor3 = Color3.fromRGB(45, 40, 45)
content.BackgroundTransparency = 0.15
content.BorderSizePixel = 0
content.Parent = mainFrame
Instance.new("UICorner", content).CornerRadius = UDim.new(0, 8)

-- Botões estilo mac
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(0, 45, 0, 14)
topBar.Position = UDim2.new(1, -55, 0, 5)
topBar.BackgroundTransparency = 1
topBar.Parent = mainFrame

local colors = {
    Color3.fromRGB(255, 95, 86),
    Color3.fromRGB(255, 189, 46),
    Color3.fromRGB(39, 201, 63)
}

for i, col in ipairs(colors) do
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0, 10, 0, 10)
    dot.Position = UDim2.new(0, (i - 1) * 15, 0, 0)
    dot.BackgroundColor3 = col
    dot.BorderSizePixel = 0
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
    dot.Parent = topBar
end

-- Função do botão Auto Farm
autoFarmBtn.MouseButton1Click:Connect(function()
    print("Auto Farm ativado!") -- Coloque o código aqui
end)

-- Sistema de abrir/fechar com tecla G
local visible = true
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.G then
        visible = not visible
        mainFrame.Visible = visible
    end
end)
